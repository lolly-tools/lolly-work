{
  "name": "@lolly/engine",
  "version": "1.197.0",
  "//version": "Tracks ENGINE_VERSION in src/version.ts - the published package version IS the engine version. scripts/pack-engine.ts asserts they match before packing.",
  "description": "Platform-agnostic engine for Lolly. Loads tool manifests, renders inputs, hydrates templates, drives exports through the capability bridge.",
  "license": "MPL-2.0",
  "type": "module",
  "files": [
    "src",
    "CHANGELOG.md"
  ],
  "//sideEffects": "The engine is pure - every module only defines/exports; none patch globals or run required top-level effects. Declaring this lets the bundler tree-shake the barrel (src/index.ts) so importing e.g. `parseUrlState` no longer drags createRuntime→Handlebars and loadTool→Ajv onto the web shell's boot chunk. See plans/archive/ui-perf-audit.md.",
  "sideEffects": false,
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./bridge/v1": "./src/bridge/host-v1.ts"
  },
  "dependencies": {
    "@lolly-tools/core": "1.0.0",
    "ajv": "^8.17.1",
    "css-tree": "^3.2.1",
    "fflate": "^0.8.3",
    "handlebars": "^4.7.8",
    "prettier": "^3.9.6",
    "smol-toml": "^1.8.0",
    "sql-formatter": "^15.8.2",
    "terser": "^5.51.2",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@types/css-tree": "^3.2.0",
    "typescript": "^7.0.2"
  },
  "//": "The engine has NO direct dependency on a DOM library, framework, or storage backend. Those are provided by the host shell at runtime via the capability bridge.",
  "scripts": {
    "test": "node --test \"../tests/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  }
}